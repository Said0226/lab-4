#include<iostream> 
using namespace std; 
int linearsearch(int arr[],int size ,int target) 
{ 
} 
for( int i=0; i<size; i++) 
{ 
} 
if(arr[i]==target) 
{ 
} 
return -1; 
int main() 
{ 
int arr[10]; 
int size; 
return i; 
cout<<"enter the size of array" ; 
cin>>size; 
for(int i = 0; i<size; i++) 
{ 
cout<<"enter the array elements at index: "<<i<<"\t" ; 
cin>>arr[i]; 
} 
} 
size = sizeof(arr) / sizeof(arr[0]); 
int target; 
cout<<"enter the value of target to be search"<<"\t" ; 
cin>>target; 
int result = linearsearch(arr, size, target); 
if(result  != -1) 
{ 
} 
else 
{ 
} 
cout<<"element" <<target <<" fount "<<result<<endl; 
cout<<"element"<< target<<" not found" <<endl;
